<footer aria-hidden="true"><span></span></footer>

<style>
  footer {
    width: min(calc(100% - 64px), 920px);
    height: 34px;
    margin: 72px auto 20px;
    overflow: hidden;
    border: 1px solid rgba(255,255,255,.36);
    border-radius: 999px;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,.32) 22% 78%, transparent);
    box-shadow: inset 0 1px rgba(255,255,255,.48), 0 7px 22px rgba(29,103,137,.08);
    mask-image: linear-gradient(90deg, transparent, #000 16% 84%, transparent);
    backdrop-filter: blur(10px) saturate(1.3);
  }

  span {
    display: block;
    width: 54%;
    height: 1px;
    margin: 16px auto 0;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,.82), transparent);
  }
</style>
